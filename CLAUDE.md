# tmux Session Switcher - 仕様書

**バージョン:** 1.0  
**作成日:** 2024年  
**対象:** tmux 3.2以上  

---

## 目次

1. [プロジェクト概要](#1-プロジェクト概要)
2. [要件定義](#2-要件定義)
3. [システムアーキテクチャ](#3-システムアーキテクチャ)
4. [機能仕様](#4-機能仕様)
5. [UI/UX仕様](#5-uiux仕様)
6. [技術仕様](#6-技術仕様)
7. [ファイル構成](#7-ファイル構成)
8. [各コンポーネント詳細仕様](#8-各コンポーネント詳細仕様)
9. [設定仕様](#9-設定仕様)
10. [インストール・セットアップ](#10-インストールセットアップ)
11. [テスト仕様](#11-テスト仕様)
12. [運用・保守](#12-運用保守)
13. [今後の拡張計画](#13-今後の拡張計画)

---

## 1. プロジェクト概要

### 1.1 目的

tmuxのセッション管理を視覚的かつ効率的に行うためのプラグイン。lazygitのようなポップアップUIを提供し、セッション・ウィンドウ・ペーンの階層的なナビゲーションとリアルタイムプレビューを実現する。

### 1.2 背景

- tmuxの標準セッション切り替え（`prefix + s`）は視認性が低い
- 複数セッション・ウィンドウの管理が煩雑
- 切り替え前に内容を確認できない
- セッション操作（作成・削除・リネーム）のUXが悪い

### 1.3 ターゲットユーザー

- 複数tmuxセッションを使用する開発者
- ウィンドウ・ペーンを多用するユーザー
- 視覚的なUIを好むユーザー

### 1.4 成功指標

- セッション切り替え時間の短縮（50%削減）
- ユーザー満足度向上
- tmux操作の学習コスト削減

---

## 2. 要件定義

### 2.1 機能要件

#### 必須機能（MUST）

| ID | 機能 | 説明 |
|----|------|------|
| F-001 | セッション一覧表示 | 全tmuxセッションをリスト形式で表示 |
| F-002 | セッション詳細表示 | 選択セッションのウィンドウ・ペーン構成を表示 |
| F-003 | ペイン内容プレビュー | ポップアップ内で他セッションのペーン内容を表示 |
| F-004 | セッション切り替え | Enter押下でセッションに切り替え |
| F-005 | セッション新規作成 | Ctrl-nで新規セッション作成 |
| F-006 | セッション名変更 | Ctrl-rでセッション名変更 |
| F-007 | セッション削除 | Ctrl-xでセッション削除（確認あり） |
| F-008 | ウィンドウ一覧表示 | セッション内の全ウィンドウを表示 |
| F-009 | ウィンドウ切り替え | ウィンドウへの直接切り替え |
| F-010 | ペーン一覧表示 | ウィンドウ内の全ペーンを表示 |
| F-011 | ペーン切り替え | ペーンへの直接切り替え |
| F-012 | 階層的ナビゲーション | セッション→ウィンドウ→ペーンの3階層移動 |

#### 推奨機能（SHOULD）

| ID | 機能 | 説明 |
|----|------|------|
| F-101 | ファジー検索 | セッション名のインクリメンタル検索 |
| F-102 | プレビュー切替 | Ctrl-/でプレビューの表示/非表示 |
| F-103 | カラーテーマ | Tokyo Night / Catppuccin / Default |
| F-104 | ソート機能 | 作成日時・最終使用・名前順ソート |
| F-105 | フィルタリング | アタッチ済み/デタッチのみ表示 |

#### 任意機能（COULD）

| ID | 機能 | 説明 |
|----|------|------|
| F-201 | セッション複製 | 既存セッションの複製 |
| F-202 | ブックマーク機能 | よく使うセッションをマーク |
| F-203 | 統計情報 | セッション使用時間・頻度の表示 |

### 2.2 非機能要件

#### 性能要件

| ID | 項目 | 要件 | 検証方法 |
|----|------|------|-----------|
| NF-001 | 起動時間 | ポップアップ表示まで0.3秒以内 | `tmux display-popup`起動を100回計測し、`/usr/bin/time -p`の平均をCIに保存 |
| NF-002 | プレビュー生成 | ペーン内容取得まで0.5秒以内 | `scripts/preview-session.sh`を高負荷セッションで実行し、ms単位のログを取得 |
| NF-003 | リスト更新 | セッション一覧更新0.2秒以内 | `scripts/session-list.sh`を`time`付きで実行し、結果をテストログへ出力 |
| NF-004 | メモリ使用量 | 10MB以下 | `/usr/bin/time -v`(Linux)/`time -l`(macOS)で最大常駐サイズを収集し、閾値超過で失敗 |

#### 互換性要件

| ID | 項目 | 要件 | 検証方法 |
|----|------|------|-----------|
| NF-101 | tmuxバージョン | 3.2以上（popup機能必須） | `tmux -V`をCIで確認し、version compare関数でチェック |
| NF-102 | OS | Linux / macOS | GitHub ActionsでUbuntu/macOS matrix統合テストを実行 |
| NF-103 | シェル | bash 4.0以上 | `bash --version`を起動ログへ出力し、未満ならFATAL終了 |
| NF-104 | 依存ツール | fzf 0.30以上 | `fzf --version`の結果をログし、条件未達はエラー表示 |

#### 保守性要件

| ID | 項目 | 要件 | 検証方法 |
|----|------|------|-----------|
| NF-201 | コード可読性 | 関数単位で分割、コメント必須 | `shellcheck` CIとPRレビュー Checklistで確認 |
| NF-202 | 設定の柔軟性 | tmux.confで全設定変更可能 | `.tmux.conf`カスタム例で統合テストを実行し、設定が反映されるか検証 |
| NF-203 | エラーハンドリング | 全エラーケースでgraceful degradation | エラーシナリオ自動テスト＋`TMUX_SESSION_SWITCHER_DEBUG=1`でログにWARN/ERRORが出ることを検証 |

---

## 3. システムアーキテクチャ

### 3.1 全体構成

```
┌─────────────────────────────────────────────────────────────┐
│                         ユーザー                              │
└───────────────────────┬─────────────────────────────────────┘
                        │ Ctrl-s
                        ↓
┌─────────────────────────────────────────────────────────────┐
│                    tmux (Plugin Host)                        │
│  ┌───────────────────────────────────────────────────────┐  │
│  │          tmux display-popup (Container)               │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │              fzf (UI Layer)                      │  │  │
│  │  │  ┌───────────┐  ┌──────────────────────────┐   │  │  │
│  │  │  │ List View │  │   Preview Window         │   │  │  │
│  │  │  └───────────┘  └──────────────────────────┘   │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────┐
│                  Shell Scripts (Logic Layer)                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │ session-mode │→ │ detail-mode  │→ │  pane-mode   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │session-list.sh│ │window-list.sh│  │ pane-list.sh │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │preview-sess. │  │preview-window│  │preview-pane  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ↓
┌─────────────────────────────────────────────────────────────┐
│                   tmux Commands (Data Layer)                 │
│  • list-sessions  • list-windows  • list-panes              │
│  • capture-pane   • switch-client • kill-session            │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 データフロー

```
ユーザー操作
    ↓
キーバインド (Ctrl-s)
    ↓
switcher.sh (エントリーポイント)
    ↓
session-mode.sh
    ├→ session-list.sh (データ取得)
    │   └→ tmux list-sessions
    │
    └→ preview-session.sh (プレビュー生成)
        └→ tmux list-windows, capture-pane
    ↓
fzf (ユーザー選択)
    ↓
[Enter押下]
    ↓
tmux switch-client (切り替え実行)

[Space押下]
    ↓
detail-mode.sh
    ├→ window-list.sh
    └→ preview-window.sh
        ↓
    [Space押下]
        ↓
    pane-mode.sh
        ├→ pane-list.sh
        └→ preview-pane.sh
```

### 3.3 モジュール構成

| モジュール名 | 役割 | 入力 | 出力 |
|-------------|------|------|------|
| switcher.sh | エントリーポイント | - | popup起動 |
| session-mode.sh | セッション選択UI | - | 選択セッション名 |
| detail-mode.sh | ウィンドウ選択UI | セッション名 | 選択ウィンドウindex |
| pane-mode.sh | ペーン選択UI | セッション名, ウィンドウindex | 選択ペーンindex |
| session-list.sh | セッション一覧生成 | - | フォーマット済みリスト |
| window-list.sh | ウィンドウ一覧生成 | セッション名 | フォーマット済みリスト |
| pane-list.sh | ペーン一覧生成 | セッション名, ウィンドウindex | フォーマット済みリスト |
| preview-session.sh | セッションプレビュー | セッション名 | ANSI装飾テキスト |
| preview-window.sh | ウィンドウプレビュー | セッション名, ウィンドウindex | ANSI装飾テキスト |
| preview-pane.sh | ペーンプレビュー | セッション名, ウィンドウindex, ペーンindex | ANSI装飾テキスト |
| actions.sh | CRUD操作 | アクション, パラメータ | 操作結果 |

---

## 4. 機能仕様

### 4.1 セッション一覧表示（F-001）

#### 入力
- なし（現在のtmux環境から自動取得）

#### 処理
1. `tmux list-sessions`コマンド実行
2. 各セッションの情報を取得：
   - セッション名
   - ウィンドウ数
   - アタッチ状態
   - 作成時刻
   - 最終活動時刻
3. フォーマット処理
4. ソート（現在→アタッチ→デタッチ）

#### 出力形式
```
[アイコン] [セッション名(20文字)]  [活動マーク] [ウィンドウ数] [経過時間]
```

#### 例
```
📝 dev-project          🔥 [ 5W]    2h
📎 backend              ⚡ [ 2W]   30m
💤 dotfiles                [ 1W]    3d
```

#### アイコン仕様
- 📝: 現在のセッション（緑色）
- 📎: アタッチ済み（黄色）
- 💤: デタッチ（グレー）

#### 活動マーク仕様
- 🔥: 5分以内に活動
- ⚡: 1時間以内に活動
- (空白): それ以上

### 4.2 セッション詳細表示（F-002）

#### 入力
- セッション名（fzfで選択中の行から抽出）

#### 処理
1. セッション基本情報取得
2. ウィンドウ一覧取得
3. アクティブウィンドウのペーン情報取得
4. フォーマット処理

#### 出力構成
```
╔════════════════════════════════════╗
║ 📦 Session: [セッション名]
╚════════════════════════════════════╝

┌─ Info
├─ Status:       [attached/detached]
├─ Windows:      [数値]
├─ Created:      [経過時間]
└─ Last Activity: [経過時間]

┌─ Windows
│ [idx]: [ウィンドウ名]     [ペーン数]
│ ...

┌─ Active Window Preview: [ウィンドウ名]
│ [ペーン内容 15行]
└─ (Showing first 15 lines of pane 0)
```

### 4.3 ペイン内容プレビュー（F-003）

#### 入力
- セッション名
- ウィンドウindex
- ペーンindex

#### 処理
1. `tmux capture-pane`でペーン内容取得
2. 最新50行を抽出
3. ANSI装飾付きでフォーマット

#### 出力
- ボーダー付きボックス
- 行番号なし
- 元のANSIカラー保持

#### 制約
- 最大取得行数: 50行
- バイナリデータは非対応
- スクロールバックバッファに依存

### 4.4 セッション新規作成（F-005）

#### トリガー
- Ctrl-n押下

#### 処理フロー
1. fzfで入力プロンプト表示
2. セッション名入力
3. 重複チェック
4. `tmux new-session -d -s [名前]`実行
5. セッション一覧を自動リロード

#### バリデーション
- 空文字チェック
- 重複チェック
- 特殊文字制限（`.`、`:`は使用不可）

#### エラーハンドリング
```bash
if tmux has-session -t "$NEW_NAME" 2>/dev/null; then
    tmux display-message "Session '$NEW_NAME' already exists"
    exit 1
fi
```

### 4.5 セッション名変更（F-006）

#### トリガー
- Ctrl-r押下

#### 処理フロー
1. 現在選択中のセッション名を取得
2. fzfで新しい名前を入力（初期値は現在の名前）
3. 重複チェック
4. `tmux rename-session`実行
5. セッション一覧を自動リロード

#### バリデーション
- F-005と同様

### 4.6 セッション削除（F-007）

#### トリガー
- Ctrl-x押下

#### 処理フロー
1. 確認プロンプト表示（fzf）
2. "Yes"/"No"選択
3. 最後のセッションチェック
4. 現在のセッションチェック
5. 削除実行
6. 必要なら他セッションへ切り替え

#### 安全機構
```bash
# 最後のセッション保護
if [ "$SESSION_COUNT" -eq 1 ]; then
    tmux display-message "Cannot kill the last session"
    exit 1
fi

# 現在のセッション削除時は自動切り替え
if [ "$SESSION_NAME" = "$CURRENT" ]; then
    OTHER_SESSION=$(tmux list-sessions -F "#{session_name}" | grep -v "^${SESSION_NAME}$" | head -1)
    tmux switch-client -t "$OTHER_SESSION"
    tmux kill-session -t "$SESSION_NAME"
fi
```

### 4.7 階層的ナビゲーション（F-012）

#### 遷移図
```
Session Mode
    ↓ [Space]
Window Mode
    ↓ [Space]
Pane Mode
    ↑ [ESC]
Window Mode
    ↑ [ESC]
Session Mode
```

#### 各階層の操作

**Session Mode**
- Enter: セッション切り替え
- Space: ウィンドウモードへ
- Ctrl-n/r/x: セッション操作
- q: 終了

**Window Mode**
- Enter: ウィンドウ切り替え
- Space: ペーンモードへ
- ESC: セッションモードへ戻る

**Pane Mode**
- Enter: ペーン切り替え
- ESC: ウィンドウモードへ戻る

---

## 5. UI/UX仕様

### 5.1 レイアウト仕様

#### ポップアップサイズ
- デフォルト幅: 95%
- デフォルト高さ: 90%
- 最小推奨サイズ: 100x30文字

#### 画面分割
```
┌────────────────────────────────────────────────────┐
│ Header (1行)                                        │
├─────────────────┬──────────────────────────────────┤
│ List (35%)      │ Preview (65%)                    │
│                 │                                  │
│ (可変)          │ (可変)                           │
│                 │                                  │
├─────────────────┴──────────────────────────────────┤
│ Footer (1行)                                        │
└────────────────────────────────────────────────────┘
```

#### プレビューウィンドウ設定
```bash
--preview-window="right:65%:wrap:border-left"
```
- 位置: 右
- 幅: 65%
- 折り返し: 有効
- ボーダー: 左のみ

### 5.2 カラーテーマ仕様

#### Tokyo Night
```bash
--color="bg+:#364a82,bg:#1a1b26,border:#7aa2f7"
--color="fg:#c0caf5,fg+:#c0caf5,hl:#7aa2f7,hl+:#7dcfff"
--color="info:#7aa2f7,prompt:#7dcfff,pointer:#f7768e"
--color="marker:#9ece6a,spinner:#9ece6a,header:#9ece6a"
```

| 要素 | 色コード | 説明 |
|------|---------|------|
| bg | #1a1b26 | 背景色 |
| bg+ | #364a82 | 選択行背景 |
| fg | #c0caf5 | テキスト色 |
| border | #7aa2f7 | ボーダー色 |
| pointer | #f7768e | カーソル色 |
| marker | #9ece6a | マーカー色 |

#### Catppuccin
```bash
--color="bg+:#313244,bg:#1e1e2e,border:#89b4fa"
--color="fg:#cdd6f4,fg+:#cdd6f4,hl:#89b4fa,hl+:#94e2d5"
--color="info:#89b4fa,prompt:#94e2d5,pointer:#f38ba8"
--color="marker:#a6e3a1,spinner:#a6e3a1,header:#a6e3a1"
```

※ テーマファイルが見つからない場合は`themes/default.sh`を自動読み込みし、ログにWARNを出力する。

### 5.3 キーバインド一覧

#### 共通操作
| キー | 動作 | 対象モード |
|------|------|-----------|
| j / ↓ | 下へ移動 | 全モード |
| k / ↑ | 上へ移動 | 全モード |
| Ctrl-u | 半ページ上 | 全モード |
| Ctrl-d | 半ページ下 | 全モード |
| g | 先頭へ | 全モード |
| G | 末尾へ | 全モード |
| / | 検索 | 全モード |
| Ctrl-/ | プレビュー切替 | 全モード |
| q | 終了 | 全モード |

#### Session Mode専用
| キー | 動作 |
|------|------|
| Enter | セッション切り替え |
| Space | ウィンドウモードへ |
| Ctrl-n | 新規セッション作成 |
| Ctrl-r | セッション名変更 |
| Ctrl-x | セッション削除 |

#### Window Mode専用
| キー | 動作 |
|------|------|
| Enter | ウィンドウ切り替え |
| Space | ペーンモードへ |
| ESC | セッションモードへ |

#### Pane Mode専用
| キー | 動作 |
|------|------|
| Enter | ペーン切り替え |
| ESC | ウィンドウモードへ |

### 5.4 ヘッダー・フッター仕様

#### Session Mode Header
```
┃ ⏎ Switch │ ␣ Details │ Ctrl-n New │ Ctrl-r Rename │ Ctrl-x Delete │ Ctrl-/ Preview │ q Quit ┃
```

#### Window Mode Header
```
┃ Session: [セッション名] │ ⏎ Switch │ ␣ Pane Details │ ESC Back │ Ctrl-/ Preview ┃
```

#### Pane Mode Header
```
┃ Window: [セッション名]:[ウィンドウindex] │ ⏎ Switch │ ESC Back │ Ctrl-/ Preview ┃
```

### 5.5 プロンプト表示

| モード | プロンプト |
|--------|----------|
| Session | 🔍 Sessions > |
| Window | 🪟 Windows > |
| Pane | 🖼️  Panes > |

### 5.6 アニメーション仕様

fzfの標準機能を使用：
- リスト更新時: スムーズなフェード
- プレビュー切替: 即座に表示/非表示
- 選択移動: ハイライトのスムーズな移動

---

## 6. 技術仕様

### 6.1 依存関係

#### 必須
| ツール | 最小バージョン | 用途 |
|--------|---------------|------|
| tmux | 3.2 | ホスト環境（popup必須） |
| bash | 4.0 | スクリプト実行 |
| fzf | 0.30.0 | UI・検索機能 |
| GNU coreutils | - | date, grep, awk等 |

#### 推奨
| ツール | 用途 |
|--------|------|
| bat | シンタックスハイライト（将来） |
| eza/exa | ファイルリスト表示（将来） |

### 6.2 tmux APIコマンド仕様

#### list-sessions
```bash
tmux list-sessions -F "#{session_name}|#{session_windows}|#{session_attached}|#{session_created}|#{session_activity}"
```

**出力フォーマット:**
```
session1|5|1|1699000000|1699001000
session2|2|0|1698000000|1698900000
```

**使用フィールド:**
- `session_name`: セッション名
- `session_windows`: ウィンドウ数
- `session_attached`: アタッチ状態（1=attached）
- `session_created`: 作成時刻（Unix timestamp）
- `session_activity`: 最終活動時刻（Unix timestamp）

#### list-windows
```bash
tmux list-windows -t "$SESSION_NAME" -F "#{window_index}|#{window_name}|#{window_panes}|#{window_active}|#{window_layout}"
```

**出力フォーマット:**
```
0|editor|3|1|layout_string
1|terminal|1|0|layout_string
```

#### list-panes
```bash
tmux list-panes -t "${SESSION_NAME}:${WINDOW_INDEX}" -F "#{pane_index}|#{pane_current_command}|#{pane_width}|#{pane_height}|#{pane_active}|#{pane_pid}"
```

**出力フォーマット:**
```
0|nvim|120|30|1|12345
1|bash|120|10|0|12346
```

#### capture-pane
```bash
tmux capture-pane -t "${SESSION_NAME}:${WINDOW_INDEX}.${PANE_INDEX}" -p -S -
```

**オプション:**
- `-p`: 標準出力へ出力
- `-S -`: スクロールバックバッファ全体

### 6.3 fzf統合仕様

#### 基本オプション
```bash
fzf \
  --ansi \              # ANSIカラーコード有効
  --border=rounded \     # 角丸ボーダー
  --height=100% \        # 全画面
  --preview="..." \      # プレビューコマンド
  --preview-window="..." # プレビューウィンドウ設定
```

#### バインディング構文
```bash
--bind="key:action"
```

**主要アクション:**
- `execute(command)`: コマンド実行
- `reload(command)`: リスト再読み込み
- `become(command)`: fzf終了後にコマンド実行
- `abort`: fzf終了
- `toggle-preview`: プレビュー表示切替
- `preview-page-up/down`: プレビュースクロール

#### expect機能
```bash
--expect="enter,space"
```
押下されたキーを出力の最初の行に含める

### 6.4 エラーハンドリング

#### エラーレベル定義
| レベル | 動作 |
|--------|------|
| FATAL | プラグイン起動不可、エラーメッセージ表示 |
| ERROR | 操作失敗、メッセージ表示、継続可能 |
| WARN | 警告表示、動作継続 |
| INFO | 情報表示 |

#### エラーケース

**FATAL:**
- tmux 3.2未満
- fzf未インストール
- bash 4.0未満

**ERROR:**
- セッション作成失敗（重複名）
- セッション削除失敗（最後のセッション）
- 存在しないセッションへのアクセス

**WARN:**
- プレビュー生成失敗（ペーンが存在しない）
- タイムアウト

#### エラーメッセージ
```bash
tmux display-message "[Error] Message text"
```

### 6.5 パフォーマンス最適化

#### キャッシング戦略
- セッション一覧: キャッシュなし（常に最新）
- プレビュー: 生成時のみ（fzfのキャッシュ機能）

#### 並列処理
```bash
# 複数セッションのプレビュー生成を並列化（将来実装）
for session in $sessions; do
    preview_session "$session" &
done
wait
```

#### 最適化ポイント
1. パイプライン最適化（不要な`cat`削除）
2. 外部コマンド呼び出し削減
3. 文字列処理の効率化（sedよりbash組み込み機能）

---

## 7. ファイル構成

```
tmux-session-switcher/
├── README.md                          # プロジェクト説明
├── LICENSE                            # ライセンス（MIT推奨）
├── .gitignore                         # Git除外設定
│
├── tmux-session-switcher.tmux         # tmuxプラグインエントリー
│
├── scripts/
│   ├── switcher.sh                    # メインエントリーポイント
│   ├── session-mode.sh                # セッション選択モード
│   ├── detail-mode.sh                 # ウィンドウ選択モード
│   ├── pane-mode.sh                   # ペーン選択モード
│   │
│   ├── session-list.sh                # セッション一覧生成
│   ├── window-list.sh                 # ウィンドウ一覧生成
│   ├── pane-list.sh                   # ペーン一覧生成
│   │
│   ├── preview-session.sh             # セッションプレビュー
│   ├── preview-window.sh              # ウィンドウプレビュー
│   ├── preview-pane.sh                # ペーンプレビュー
│   │
│   ├── actions.sh                     # CRUD操作
│   └── utils.sh                       # 共通ユーティリティ
│
├── themes/                            # カラーテーマ定義
│   ├── default.sh
│   ├── tokyonight.sh
│   └── catppuccin.sh
│
├── docs/
│   ├── SPECIFICATION.md               # 本仕様書
│   ├── ARCHITECTURE.md                # アーキテクチャ詳細
│   ├── DEVELOPMENT.md                 # 開発ガイド
│   └── images/                        # スクリーンショット・図
│
└── tests/                             # テストスクリプト（将来）
    ├── test-session-mode.sh
    ├── test-actions.sh
    └── fixtures/                      # テストデータ
```

### ファイルサイズ目安
| ファイル | 行数 | 目安 |
|---------|------|------|
| session-mode.sh | ~80行 | 中規模 |
| session-list.sh | ~50行 | 小規模 |
| preview-session.sh | ~70行 | 中規模 |
| actions.sh | ~100行 | 中規模 |

---

## 8. 各コンポーネント詳細仕様

### 8.1 switcher.sh

**責務:** プラグインのエントリーポイント

**処理フロー:**
1. 環境変数読み込み
2. tmux popup起動
3. session-mode.sh実行

**実装:**
```bash
#!/usr/bin/env bash

CURRENT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 設定読み込み
POPUP_WIDTH="${TMUX_SESSION_SWITCHER_POPUP_WIDTH:-95%}"
POPUP_HEIGHT="${TMUX_SESSION_SWITCHER_POPUP_HEIGHT:-90%}"

# Popup起動
tmux display-popup -E -w "$POPUP_WIDTH" -h "$POPUP_HEIGHT" \
    "bash $CURRENT_DIR/session-mode.sh"
```

**エラーハンドリング:**
- tmux 3.2未満の場合、エラーメッセージ表示
- popup起動失敗時、フォールバック（通常ペインで起動）

### 8.2 session-mode.sh

**責務:** セッション選択UIの制御

**入力:** なし

**出力:** 
- 選択されたセッション名（Enter押下時）
- detail-mode.shへの遷移（Space押下時）

**処理フロー:**
1. テーマ設定ロード
2. fzfオプション構築
3. session-list.sh実行してリスト生成
4. fzf起動
5. ユーザー操作待機
6. 結果処理

**fzfバインディング:**
```bash
--bind="ctrl-n:execute($CURRENT_DIR/actions.sh new)+reload($CURRENT_DIR/session-list.sh)"
--bind="ctrl-r:execute($CURRENT_DIR/actions.sh rename {1})+reload($CURRENT_DIR/session-list.sh)"
--bind="ctrl-x:execute($CURRENT_DIR/actions.sh kill {1})+reload($CURRENT_DIR/session-list.sh)"
--bind="space:execute(bash $CURRENT_DIR/detail-mode.sh {1})+abort"
--bind="enter:become(echo switch {1})"
```

**パラメータ抽出:**
```bash
# {1} = 選択行の最初のフィールド（セッション名）
SESSION=$(echo "$RESULT" | tail -1 | awk '{print $1}')
```

### 8.3 session-list.sh

**責務:** セッション一覧のフォーマット済みテキスト生成

**入力:** なし

**出力:** ANSIカラーコード付きテキスト（標準出力）

**処理詳細:**
```bash
# 1. tmuxからデータ取得
tmux list-sessions -F "#{session_name}|#{session_windows}|#{session_attached}|#{session_created}|#{session_activity}"

# 2. 現在のセッション取得
CURRENT_SESSION=$(tmux display-message -p '#S')

# 3. 各行を処理
while IFS='|' read -r name windows attached created activity; do
    # アイコン決定
    if [ "$name" = "$CURRENT_SESSION" ]; then
        icon="📝"; color="\033[1;32m"
    elif [ "$attached" -gt 0 ]; then
        icon="📎"; color="\033[1;33m"
    else
        icon="💤"; color="\033[2;37m"
    fi
    
    # 時間計算
    now=$(date +%s)
    created_ago=$(( (now - created) / 60 ))
    
    # フォーマット出力
    printf "${color}%-20s\033[0m %s ..." "$name" "$icon"
done
```

**出力フォーマット:**
```
[色付きセッション名(20文字固定)] [アイコン] [活動マーク] [ウィンドウ数] [経過時間]
```

**ソート順:**
1. 現在のセッション（📝）
2. アタッチ済みセッション（📎）
3. デタッチセッション（💤）

### 8.4 preview-session.sh

**責務:** セッション詳細情報の生成

**入力:** セッション名（$1）

**出力:** ANSIカラーコード付きテキスト

**処理フロー:**
1. ヘッダー生成（ボックス装飾）
2. セッション基本情報表示
3. ウィンドウ一覧表示
4. アクティブウィンドウのプレビュー生成

**実装のポイント:**
```bash
# ボックス装飾
echo -e "\033[1;35m╔════════════════════╗\033[0m"
echo -e "\033[1;35m║\033[0m \033[1;36m📦 Session: $SESSION_NAME\033[0m"
echo -e "\033[1;35m╚════════════════════╝\033[0m"

# ウィンドウリスト
tmux list-windows -t "$SESSION_NAME" -F "..." | \
while IFS='|' read -r idx name panes active; do
    if [ "$active" = "1" ]; then
        marker="❯"; color="\033[1;32m"
    else
        marker="│"; color="\033[2m"
    fi
    printf "${color}%s %2d: %-30s [%dP]\033[0m\n" "$marker" "$idx" "$name" "$panes"
done

# ペイン内容プレビュー
tmux capture-pane -t "${SESSION_NAME}:${ACTIVE_WINDOW}.0" -p | head -15
```

### 8.5 detail-mode.sh

**責務:** ウィンドウ選択UIの制御

**入力:** セッション名（$1）

**出力:** 選択されたウィンドウindex

**処理フロー:**
1. セッション名バリデーション
2. window-list.sh実行
3. fzf起動（プレビューはpreview-window.sh）
4. ユーザー操作待機
5. 結果処理

**fzfバインディング:**
```bash
--bind="space:execute(bash $CURRENT_DIR/pane-mode.sh $SESSION_NAME {1})+abort"
--bind="enter:become(echo switch $SESSION_NAME {1})"
```

**ヘッダー:**
```bash
--header="┃ Session: $SESSION_NAME │ ⏎ Switch │ ␣ Pane Details │ ESC Back │ Ctrl-/ Preview ┃"
```

### 8.6 pane-mode.sh

**責務:** ペーン選択UIの制御

**入力:** 
- セッション名（$1）
- ウィンドウindex（$2）

**出力:** 選択されたペーンindex

**処理フロー:**
1. パラメータバリデーション
2. pane-list.sh実行
3. fzf起動（プレビューはpreview-pane.sh）
4. ユーザー操作待機
5. 結果処理

**切り替えコマンド:**
```bash
tmux switch-client -t "${SESSION_NAME}:${WINDOW_INDEX}.${PANE_INDEX}"
```

### 8.7 actions.sh

**責務:** セッションのCRUD操作

**入力:**
- アクション種別（$1）: new / rename / kill
- セッション名（$2）: rename / kill時

**処理詳細:**

#### new（新規作成）
```bash
# 1. 名前入力プロンプト
NEW_NAME=$(echo "" | fzf --print-query --prompt="New session name: " ...)

# 2. 重複チェック
if tmux has-session -t "$NEW_NAME" 2>/dev/null; then
    tmux display-message "Session '$NEW_NAME' already exists"
    exit 1
fi

# 3. セッション作成
tmux new-session -d -s "$NEW_NAME"
tmux display-message "Created session '$NEW_NAME'"
```

#### rename（名前変更）
```bash
# 1. 新しい名前入力（初期値=現在の名前）
NEW_NAME=$(echo "$SESSION_NAME" | fzf --print-query ...)

# 2. バリデーション
if [ "$NEW_NAME" = "$SESSION_NAME" ]; then
    exit 0  # 変更なし
fi

if tmux has-session -t "$NEW_NAME" 2>/dev/null; then
    tmux display-message "Session '$NEW_NAME' already exists"
    exit 1
fi

# 3. リネーム実行
tmux rename-session -t "$SESSION_NAME" "$NEW_NAME"
```

#### kill（削除）
```bash
# 1. 確認プロンプト
CONFIRM=$(echo -e "No\nYes" | fzf --prompt="Kill session '$SESSION_NAME'? " ...)

if [ "$CONFIRM" != "Yes" ]; then
    exit 0
fi

# 2. 最後のセッションチェック
SESSION_COUNT=$(tmux list-sessions | wc -l)
if [ "$SESSION_COUNT" -eq 1 ]; then
    tmux display-message "Cannot kill the last session"
    exit 1
fi

# 3. 現在のセッションの場合は切り替えてから削除
CURRENT=$(tmux display-message -p '#S')
if [ "$SESSION_NAME" = "$CURRENT" ]; then
    OTHER_SESSION=$(tmux list-sessions -F "#{session_name}" | grep -v "^${SESSION_NAME}$" | head -1)
    tmux switch-client -t "$OTHER_SESSION"
fi

# 4. 削除実行
tmux kill-session -t "$SESSION_NAME"
tmux display-message "Killed session '$SESSION_NAME'"
```

### 8.8 window-list.sh

**責務:** ウィンドウ一覧のフォーマット済みテキスト生成

**入力:** セッション名（$1）

**出力:** ANSIカラーコード付きテキスト

**処理:**
```bash
tmux list-windows -t "$SESSION_NAME" -F "#{window_index}|#{window_name}|#{window_panes}|#{window_active}|#{window_layout}" | \
while IFS='|' read -r index name panes active layout; do
    # アクティブマーカー
    if [ "$active" = "1" ]; then
        marker="❯"; color="\033[1;32m"
    else
        marker=" "; color="\033[0m"
    fi
    
    # レイアウトアイコン判定
    if [ "$panes" -eq 1 ]; then
        layout_icon="▢"
    else
        layout_icon="⊞"
    fi
    
    printf "${color}%s %2d: %-25s %s \033[2m[%dP]\033[0m\n" \
        "$marker" "$index" "$name" "$layout_icon" "$panes"
done
```

**出力例:**
```
❯  0: editor                     ⊞ [ 3P]
   1: terminal                   ▢ [ 1P]
   2: logs                       ⊞ [ 2P]
```

### 8.9 pane-list.sh

**責務:** ペーン一覧のフォーマット済みテキスト生成

**入力:**
- セッション名（$1）
- ウィンドウindex（$2）

**出力:** ANSIカラーコード付きテキスト

**処理:**
```bash
tmux list-panes -t "${SESSION_NAME}:${WINDOW_INDEX}" \
    -F "#{pane_index}|#{pane_current_command}|#{pane_width}|#{pane_height}|#{pane_active}" | \
while IFS='|' read -r index cmd width height active; do
    # アクティブマーカー
    if [ "$active" = "1" ]; then
        marker="❯"; color="\033[1;32m"
    else
        marker=" "; color="\033[0m"
    fi
    
    # コマンドアイコン
    case "$cmd" in
        vim|nvim) icon="📝" ;;
        bash|zsh|fish) icon="🐚" ;;
        ssh) icon="🔐" ;;
        *) icon="⚙️ " ;;
    esac
    
    printf "${color}%s %d: %s %-15s \033[2m(%dx%d)\033[0m\n" \
        "$marker" "$index" "$icon" "$cmd" "$width" "$height"
done
```

**出力例:**
```
❯ 0: 📝 nvim            (120x30)
  1: 🐚 bash            (120x10)
  2: 🔐 ssh             (60x40)
```

### 8.10 preview-window.sh

**責務:** ウィンドウ詳細とペーンプレビュー生成

**入力:**
- セッション名（$1）
- ウィンドウindex（$2）

**出力:** ANSIカラーコード付きテキスト

**処理フロー:**
1. ヘッダー生成
2. ウィンドウ基本情報表示
3. ペーン一覧表示
4. 各ペインの内容プレビュー（最初の8行）

**実装:**
```bash
# ペーンプレビュー
for i in $(seq 0 $((panes - 1))); do
    CMD=$(tmux display-message -t "${SESSION_NAME}:${WINDOW_INDEX}.${i}" -p "#{pane_current_command}")
    echo -e "\033[1;34m├─ Pane $i ($CMD):\033[0m"
    
    tmux capture-pane -t "${SESSION_NAME}:${WINDOW_INDEX}.${i}" -p | head -8 | \
    while IFS= read -r line; do
        echo -e "\033[1;34m│\033[0m   $line"
    done
done
```

### 8.11 preview-pane.sh

**責務:** ペーン詳細と全内容プレビュー生成

**入力:**
- セッション名（$1）
- ウィンドウindex（$2）
- ペーンindex（$3）

**出力:** ANSIカラーコード付きテキスト

**処理:**
```bash
# ペーン情報取得
PANE_INFO=$(tmux list-panes -t "${SESSION_NAME}:${WINDOW_INDEX}" \
    -F "#{pane_index}|#{pane_current_command}|#{pane_width}|#{pane_height}|#{pane_current_path}|#{pane_pid}" | \
    grep "^${PANE_INDEX}|")

# ペーン内容全体をキャプチャ（最新50行）
tmux capture-pane -t "${SESSION_NAME}:${WINDOW_INDEX}.${PANE_INDEX}" -p -S - | tail -50
```

**表示内容:**
- コマンド名
- サイズ（幅x高さ）
- カレントパス
- プロセスID
- ペーン内容（最新50行）

---

## 9. 設定仕様

### 9.1 tmux.conf設定項目

| オプション | デフォルト | 説明 | 型 |
|-----------|-----------|------|---|
| `@session-switcher-key` | `C-s` | 起動キーバインド | 文字列 |
| `@session-switcher-theme` | `tokyonight` | カラーテーマ | 文字列 |
| `@session-switcher-popup-width` | `95%` | ポップアップ幅 | 文字列 |
| `@session-switcher-popup-height` | `90%` | ポップアップ高さ | 文字列 |
| `@session-switcher-preview-width` | `65` | プレビュー幅（%） | 数値 |

### 9.2 環境変数

プラグインが設定する環境変数：

| 変数名 | 説明 | 例 |
|-------|------|---|
| `TMUX_SESSION_SWITCHER_THEME` | カラーテーマ | `tokyonight` |
| `TMUX_SESSION_SWITCHER_POPUP_WIDTH` | ポップアップ幅 | `95%` |
| `TMUX_SESSION_SWITCHER_POPUP_HEIGHT` | ポップアップ高さ | `90%` |

### 9.3 設定例

#### 最小構成
```tmux
set -g @plugin 'your-name/tmux-session-switcher'
```

#### カスタマイズ例
```tmux
set -g @plugin 'your-name/tmux-session-switcher'

# キーバインド変更
set -g @session-switcher-key 'C-j'

# テーマ変更
set -g @session-switcher-theme 'catppuccin'

# ポップアップサイズ調整
set -g @session-switcher-popup-width '80%'
set -g @session-switcher-popup-height '70%'
```

#### 複数キーバインド
```tmux
# メイン
set -g @session-switcher-key 'C-s'

# 追加バインド（手動設定）
bind-key C-j run-shell "~/.tmux/plugins/tmux-session-switcher/scripts/switcher.sh"
```

---

## 10. インストール・セットアップ

### 10.1 前提条件チェック

**必須要件:**
```bash
# tmuxバージョン確認
tmux -V  # 3.2以上

# fzfインストール確認
fzf --version  # 0.30.0以上

# bashバージョン確認
bash --version  # 4.0以上
```

### 10.2 TPMでのインストール

#### 手順
1. `.tmux.conf`に追加
```tmux
set -g @plugin 'your-name/tmux-session-switcher'
```

2. TPMでインストール
```bash
# tmux内で実行
prefix + I
```

3. 動作確認
```bash
# プラグインが起動するか確認
Ctrl-s
```

### 10.3 手動インストール

#### 手順
1. リポジトリクローン
```bash
git clone https://github.com/your-name/tmux-session-switcher.git \
    ~/.tmux/plugins/tmux-session-switcher
```

2. `.tmux.conf`に追加
```tmux
run-shell ~/.tmux/plugins/tmux-session-switcher/tmux-session-switcher.tmux
```

3. tmux設定リロード
```bash
tmux source-file ~/.tmux.conf
```

### 10.4 トラブルシューティング

#### 問題: ポップアップが表示されない
**原因:** tmux 3.2未満
**解決策:**
```bash
# tmuxバージョン確認
tmux -V

# 3.2以上にアップグレード
# macOS
brew upgrade tmux

# Ubuntu
sudo apt update
sudo apt install tmux
```

#### 問題: fzfが見つからない
**原因:** fzf未インストール
**解決策:**
```bash
# macOS
brew install fzf

# Ubuntu
sudo apt install fzf

# 手動インストール
git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf
~/.fzf/install
```

#### 問題: キーバインドが動作しない
**原因:** 既存のキーバインドと競合
**解決策:**
```tmux
# 既存のバインドを確認
tmux list-keys | grep C-s

# 別のキーに変更
set -g @session-switcher-key 'C-j'
```

---

## 11. テスト仕様

### 11.1 単体テスト

※ すべてのテストは`tmux -L session-switcher-test`など専用ソケットのtmuxサーバー上で実行し、`test-session-`接頭辞のみを安全に削除する。

#### session-list.shのテスト
```bash
#!/usr/bin/env bash
# tests/test-session-list.sh

# セットアップ
tmux new-session -d -s test1
tmux new-session -d -s test2

# テスト実行
OUTPUT=$(./scripts/session-list.sh)

# 検証
echo "$OUTPUT" | grep -q "test1" || { echo "FAIL: test1 not found"; exit 1; }
echo "$OUTPUT" | grep -q "test2" || { echo "FAIL: test2 not found"; exit 1; }

# クリーンアップ
tmux kill-session -t test1
tmux kill-session -t test2

echo "PASS: session-list.sh"
```

#### actions.shのテスト
```bash
#!/usr/bin/env bash
# tests/test-actions.sh

# 新規作成テスト
echo "test-session" | ./scripts/actions.sh new
tmux has-session -t "test-session" || { echo "FAIL: Session not created"; exit 1; }

# リネームテスト
echo "renamed-session" | ./scripts/actions.sh rename "test-session"
tmux has-session -t "renamed-session" || { echo "FAIL: Session not renamed"; exit 1; }

# 削除テスト
echo "Yes" | ./scripts/actions.sh kill "renamed-session"
! tmux has-session -t "renamed-session" || { echo "FAIL: Session not killed"; exit 1; }

echo "PASS: actions.sh"
```

### 11.2 統合テスト

#### シナリオ1: セッション切り替え
```
1. 複数セッション作成
2. Ctrl-sでポップアップ起動
3. セッション選択
4. Enterで切り替え
5. 期待: 選択したセッションに切り替わる
```

#### シナリオ2: 階層的ナビゲーション
```
1. セッション選択
2. Spaceでウィンドウモードへ
3. ウィンドウ選択
4. Spaceでペーンモードへ
5. ペーン選択
6. Enterで切り替え
7. 期待: 指定ペーンに切り替わる
```

#### シナリオ3: セッション削除
```
1. セッション選択
2. Ctrl-x押下
3. 確認プロンプトで"Yes"選択
4. 期待: セッションが削除される
5. 期待: リストから消える
```

### 11.3 パフォーマンステスト

#### 起動時間測定
```bash
#!/usr/bin/env bash

# 100回実行して平均を計算
total=0
for i in {1..100}; do
    start=$(date +%s%N)
    ./scripts/switcher.sh &
    sleep 0.1
    tmux kill-window -t popup
    end=$(date +%s%N)
    total=$(( total + (end - start) / 1000000 ))
done
average=$(( total / 100 ))
echo "Average startup time: ${average}ms"

# 期待値: 300ms以下
[ "$average" -le 300 ] || { echo "FAIL: Too slow"; exit 1; }
```

#### プレビュー生成時間測定
```bash
# 大量のウィンドウ・ペーンを持つセッションで測定
tmux new-session -d -s perf-test
for i in {1..10}; do
    tmux new-window -t perf-test
    tmux split-window -t perf-test:$i -h
    tmux split-window -t perf-test:$i -v
done

start=$(date +%s%N)
./scripts/preview-session.sh perf-test > /dev/null
end=$(date +%s%N)
time=$(( (end - start) / 1000000 ))

echo "Preview generation time: ${time}ms"

# 期待値: 500ms以下
[ "$time" -le 500 ] || { echo "FAIL: Too slow"; exit 1; }
```

### 11.4 エッジケーステスト

#### テストケース
1. **セッション名に特殊文字**
   - スペース、日本語、記号を含む名前
   - 期待: 正常に表示・操作可能

2. **大量のセッション**
   - 100個のセッションを作成
   - 期待: リストが正常に表示、スクロール可能

3. **極端に長いセッション名**
   - 100文字のセッション名
   - 期待: 適切に切り詰められる

4. **最後のセッション削除**
   - 1つのセッションのみの状態で削除試行
   - 期待: エラーメッセージ表示、削除拒否

5. **プレビュー生成失敗**
   - 存在しないセッションのプレビュー要求
   - 期待: エラーメッセージ、クラッシュなし

---

## 12. 運用・保守

### 12.1 ログ出力

#### デバッグモード
```bash
# 環境変数でデバッグモード有効化
export TMUX_SESSION_SWITCHER_DEBUG=1

# ログファイル
LOG_FILE="/tmp/tmux-session-switcher.log"

# ログ関数
log_debug() {
    if [ "$TMUX_SESSION_SWITCHER_DEBUG" = "1" ]; then
        echo "[$(date '+%Y-%m-%d %H:%M:%S')] $*" >> "$LOG_FILE"
    fi
}
```

#### ログレベル
- DEBUG: 詳細な実行情報
- INFO: 通常の動作情報
- WARN: 警告
- ERROR: エラー

### 12.2 バージョン管理

#### バージョン番号規則
```
MAJOR.MINOR.PATCH
```
- MAJOR: 互換性のない変更
- MINOR: 後方互換性のある機能追加
- PATCH: バグフィックス

#### 変更履歴
```markdown
## [1.0.0] - 2024-XX-XX
### Added
- 初回リリース
- セッション一覧表示
- 階層的ナビゲーション
- セッション管理機能

### Changed
- N/A

### Fixed
- N/A
```

### 12.3 アップデート手順

#### TPM経由
```bash
# tmux内で実行
prefix + U

# 特定プラグインのみ更新
cd ~/.tmux/plugins/tmux-session-switcher
git pull
```

#### 手動
```bash
cd ~/.tmux/plugins/tmux-session-switcher
git pull origin main

# 設定リロード
tmux source-file ~/.tmux.conf
```

### 12.4 互換性維持ガイドライン

#### 後方互換性
- 既存の設定項目は維持
- デフォルト値は変更しない
- 新機能は既存動作に影響しない

#### 非推奨機能の扱い
```bash
# 非推奨警告
if [ "$OLD_OPTION" ]; then
    tmux display-message "Warning: @old-option is deprecated. Use @new-option instead."
fi
```

---

## 13. 今後の拡張計画

### 13.1 Phase 1（基本機能）- 完成済み

- [x] セッション一覧表示
- [x] 詳細モード（ウィンドウ/ペーン）
- [x] プレビュー機能
- [x] セッション管理（CRUD）
- [x] カラーテーマ

### 13.2 Phase 2（機能拡張）

#### 13.2.1 ソート・フィルタリング
```bash
# ソートオプション追加
--bind="ctrl-s:reload(bash session-list.sh --sort=name)"
--bind="ctrl-t:reload(bash session-list.sh --sort=recent)"
--bind="ctrl-a:reload(bash session-list.sh --filter=attached)"
```

**実装項目:**
- ソート基準: 名前、作成日時、最終使用、ウィンドウ数
- フィルタ: アタッチ済み、デタッチ、最近使用

#### 13.2.2 ブックマーク機能
```bash
# ブックマーク保存ファイル
BOOKMARK_FILE="~/.tmux/session-switcher-bookmarks"

# ブックマーク追加
--bind="ctrl-b:execute(echo {1} >> $BOOKMARK_FILE)"

# ブックマーク一覧表示
--bind="ctrl-l:reload(cat $BOOKMARK_FILE | xargs -I {} bash session-list.sh --session={})"
```

#### 13.2.3 統計情報
```bash
# セッション使用統計
SESSION_STATS_FILE="~/.tmux/session-stats.json"

# 記録内容
{
  "session_name": {
    "created": "2024-01-01T00:00:00",
    "last_used": "2024-01-10T12:00:00",
    "use_count": 42,
    "total_time": 3600  # 秒
  }
}
```

### 13.3 Phase 3（UX改善）

#### 13.3.1 シンタックスハイライト
```bash
# batを使用したプレビュー
if command -v bat &> /dev/null; then
    tmux capture-pane -p | bat --color=always --style=plain
else
    tmux capture-pane -p
fi
```

#### 13.3.2 レイアウトビジュアライゼーション
```
┌────────────┬────────────┐
│            │            │
│   Pane 0   │   Pane 1   │
│            │            │
├────────────┴────────────┤
│                         │
│       Pane 2            │
│                         │
└─────────────────────────┘
```

#### 13.3.3 カスタムテーマエディタ
```toml
# ~/.tmux/session-switcher-theme.toml
[colors]
primary = "#7aa2f7"
secondary = "#9ece6a"
background = "#1a1b26"

[icons]
current_session = "📝"
attached_session = "📎"
detached_session = "💤"
```

### 13.4 Phase 4（高度な機能）

#### 13.4.1 セッションテンプレート
```yaml
# ~/.tmux/session-templates/dev.yaml
name: dev-{{project}}
windows:
  - name: editor
    layout: main-vertical
    panes:
      - command: nvim
      - command: bash
  - name: server
    panes:
      - command: npm run dev
```

#### 13.4.2 リモートセッション管理
```bash
# SSH経由でリモートtmuxを管理
--bind="ctrl-h:execute(ssh remote-host tmux list-sessions)"
```

#### 13.4.3 セッション同期
```bash
# 複数ホスト間でセッション情報を同期
sync-sessions --from localhost --to remote-host
```

### 13.5 優先度マトリクス

| 機能 | 優先度 | 工数 | 効果 |
|------|--------|------|------|
| ソート・フィルタリング | 高 | 小 | 中 |
| シンタックスハイライト | 中 | 小 | 中 |
| ブックマーク | 中 | 中 | 中 |
| 統計情報 | 低 | 中 | 低 |
| レイアウトビジュアライゼーション | 低 | 大 | 中 |
| セッションテンプレート | 低 | 大 | 高 |
| リモートセッション管理 | 低 | 大 | 低 |

---

## 付録A: 用語集

| 用語 | 説明 |
|------|------|
| Session | tmuxの最上位単位。複数のウィンドウを含む |
| Window | セッション内のタブのような単位。複数のペーンを含む |
| Pane | ウィンドウ内の分割された領域 |
| Attached | セッションに接続している状態 |
| Detached | セッションから切断している状態 |
| Popup | tmux 3.2以降の浮動ウィンドウ機能 |
| fzf | コマンドラインのファジーファインダー |
| TPM | Tmux Plugin Manager |

---

## 付録B: 参考資料

### 関連ドキュメント
- [tmux公式マニュアル](https://man.openbsd.org/tmux)
- [fzf公式ドキュメント](https://github.com/junegunn/fzf)
- [TPM公式リポジトリ](https://github.com/tmux-plugins/tpm)

### 参考プラグイン
- [tmux-resurrect](https://github.com/tmux-plugins/tmux-resurrect)
- [tmux-continuum](https://github.com/tmux-plugins/tmux-continuum)
- [extrakto](https://github.com/laktak/extrakto)

### 類似ツール
- lazygit
- lazydocker
- nnn

---

## 付録C: FAQ

### Q1: tmux 3.1以前でも使えますか？
A: いいえ、popup機能が必須なため3.2以上が必要です。

### Q2: デフォルトのキーバインドを変更できますか？
A: はい、`.tmux.conf`で変更可能です。
```tmux
set -g @session-switcher-key 'C-j'
```

### Q3: プレビューが表示されません
A: fzfのバージョンが古い可能性があります。0.30.0以上にアップグレードしてください。

### Q4: カスタムテーマを追加できますか？
A: 現在はデフォルト/TokyoNight/Catppuccinのみですが、将来的に対応予定です。

---

## 改訂履歴

| バージョン | 日付 | 変更内容 | 担当者 |
|-----------|------|---------|--------|
| 1.0 | 2024-XX-XX | 初版作成 | - |
